---
title: "Goro"
description: "One connection gives your agent search, scraping and enrichment across the real world."
---

Goro is a pay-per-call gateway. Your agent connects once, and gets a catalog of
tools that reach out to the real web: search, social profiles and posts, maps
and reviews, product and jobs data.

You hold a prepaid balance. Each call bills what it actually cost, plus our
margin. There is no subscription and no seat.

## Why it exists

Every one of these tools already exists somewhere, behind its own account, its
own key and its own bill. Wiring five of them into an agent means five
signups, five credentials and five invoices before it makes a single request.

Goro is one credential and one balance for all of them.

## How a call works

<Steps>
  <Step title="Discover">
    Ask what can answer your question. `discover` ranks the catalog against a
    plain-English query and returns each option with its price.
  </Step>
  <Step title="Inspect">
    Read the exact input a tool expects, plus a sample you can fill in.
  </Step>
  <Step title="Run">
    Make the call. Goro holds an estimate against your balance, runs the tool,
    then charges what it actually cost and releases the rest.
  </Step>
</Steps>

## What Goro does not keep

Results are not stored. Goro records what a call cost, how many rows it
returned and when it ran. The rows themselves are handed to you and never
written to our database.

That is deliberate: this catalog can reach personal data about people who have
no relationship with us, and the safest place for it is nowhere.

<Card title="Start here" icon="rocket" href="/quickstart">
  Make your first call in about five minutes.
</Card>
